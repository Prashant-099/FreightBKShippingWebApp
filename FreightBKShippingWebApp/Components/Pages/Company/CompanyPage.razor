@page "/company"

@using FreightBKShippingWebApp.Model
@using FreightBKShippingWebApp.Models
@using FreightBKShippingWebApp.Services
@using DevExpress.Blazor
@using FreightBKShippingWebApp.Components.Pages

@inject CompanyService CompanyService
@inject ToasteService ToastService
@inject LoadingService LoadingService
@inject StateService StateService
@inject CountryService CountryService
@inject EinvConfigService EinvConfigService
@inject ConfirmationDialogService ConfirmDialogService
@inject GridLayoutService GridLayoutService

<style>
    .grid-size {
        max-height: 600px;
    }

    .layout-combobox {
        border-radius: 6px;
        width: 185px;
    }
</style>

<DxGrid TItem="Company"
        Data="@companyList"
        @ref="CompanyGrid"
        KeyFieldName="CompanyId"
        ShowSearchBox="true"
        PageSize="10"
        PageSizeSelectorVisible="true"
        ShowGroupPanel="true"
        FilterMenuButtonDisplayMode="GridFilterMenuButtonDisplayMode.Always"
        FocusedRowEnabled="true"
        SelectionMode="GridSelectionMode.Multiple"
        SelectedDataItems="@SelectedCompanies"
        SelectedDataItemsChanged="OnSelectedCompaniesChanged"
        ColumnResizeMode="GridColumnResizeMode.NextColumn"
        CssClass="grid-size" AllowSelectRowByClick="true">

    <ToolbarTemplate Context="toolbarContext">
        <DxToolbar>
            <TitleTemplate Context="tl">
                <div class="d-flex align-items-center">
                    <i class="bi bi-building me-2"></i>
                    <span class="fs-5">Company</span>
                </div>
            </TitleTemplate>
            <Items>
               @* <DxToolbarItem Text="Add" IconCssClass="bi bi-plus-circle" Click="OnCreateNew" /> *@
                <DxToolbarItem Text="Edit" IconCssClass="bi bi-pencil-square" Click="OnEdit" />
                <DxToolbarItem Text="Delete" IconCssClass="bi bi-trash" Click="OnDelete" />
                <DxToolbarItem Text="Refresh" IconCssClass="bi bi-arrow-clockwise" Click="@LoadCompaniesAsync" />
                <DxToolbarItem Text="Config" IconCssClass="bi bi-gear" Click="OnConfig" />

                <DxToolbarItem BeginGroup="true" IconCssClass="bi bi-gear-wide-connected" Alignment="ToolbarItemAlignment.Right">
                    <Template>
                        <DxComboBox Data="@availableLayouts"
                                    Value="@selectedLayoutId"
                                    ValueExpression="@(() => selectedLayoutId)"
                                    ValueChanged="@((int? value) => OnLayoutSelectedAsync(value))"
                                    ValueFieldName="GridLayoutId"
                                    TextFieldName="GridLayoutName"
                                    NullText="Default View"
                                    ClearButtonDisplayMode="DataEditorClearButtonDisplayMode.Auto"
                                    CssClass="layout-combobox" />
                    </Template>
                </DxToolbarItem>

                <DxToolbarItem BeginGroup="true" Context="cfdr"
                               Alignment="ToolbarItemAlignment.Right"
                               RenderStyleMode="ToolbarItemRenderStyleMode.Contained"
                               RenderStyle="ButtonRenderStyle.Secondary"
                               IconCssClass="bi bi-gear-wide-connected">
                    <Items>
                        <DxToolbarItem Text="Save Current Layout"
                                       RenderStyle="ButtonRenderStyle.Primary"
                                       IconCssClass="bi bi-floppy"
                                       Click="ShowSaveLayoutDialog" />

                        <DxToolbarItem Text="Create New Layout"
                                       RenderStyle="ButtonRenderStyle.Success"
                                       IconCssClass="bi bi-plus-square"
                                       Click="ShowNewLayoutDialog" />

                        @if (selectedLayoutId.HasValue)
                        {
                            <DxToolbarItem Text="Delete Current Layout"
                                           RenderStyle="ButtonRenderStyle.Danger"
                                           IconCssClass="bi bi-trash"
                                           Click="DeleteCurrentLayout" />
                        }
                    </Items>
                </DxToolbarItem>
            </Items>
        </DxToolbar>
    </ToolbarTemplate>

    <Columns>
        <DxGridSelectionColumn Width="30" FixedPosition="GridColumnFixedPosition.Left" />

        <DxGridDataColumn FieldName="CompanyId" Caption="ID"
                          Visible="@GetColumnVisibility("CompanyId")"
                          Width="@GetColumnWidth("CompanyId")" />

        <DxGridDataColumn FieldName="Name" Caption="Name"
                          Visible="@GetColumnVisibility("Name")"
                          Width="@GetColumnWidth("Name")" />

        <DxGridDataColumn FieldName="Code" Caption="Code"
                          Visible="@GetColumnVisibility("Code")"
                          Width="@GetColumnWidth("Code")" />

        <DxGridDataColumn FieldName="PrintName" Caption="Print Name"
                          Visible="@GetColumnVisibility("PrintName")"
                          Width="@GetColumnWidth("PrintName")" />

        <DxGridDataColumn FieldName="Address1" Caption="Address"
                          Visible="@GetColumnVisibility("Address1")"
                          Width="@GetColumnWidth("Address1")" />

        <DxGridDataColumn FieldName="City" Caption="City"
                          Visible="@GetColumnVisibility("City")"
                          Width="@GetColumnWidth("City")" />

        <DxGridDataColumn FieldName="StateName" Caption="State"
                          Visible="@GetColumnVisibility("StateName")"
                          Width="@GetColumnWidth("StateName")" />
                          

        <DxGridDataColumn FieldName="Country" Caption="Country"
                          Visible="@GetColumnVisibility("Country")"
                          Width="@GetColumnWidth("Country")" />

        <DxGridDataColumn FieldName="Email" Caption="Email"
                          Visible="@GetColumnVisibility("Email")"
                          Width="@GetColumnWidth("Email")" />

        <DxGridDataColumn FieldName="Mobile" Caption="Mobile"
                          Visible="@GetColumnVisibility("Mobile")"
                          Width="@GetColumnWidth("Mobile")" />

        <DxGridDataColumn FieldName="Website" Caption="Website"
                          Visible="@GetColumnVisibility("Website")"
                          Width="@GetColumnWidth("Website")" />

        <DxGridDataColumn FieldName="Gstin" Caption="GSTIN"
                          Visible="@GetColumnVisibility("Gstin")"
                          Width="@GetColumnWidth("Gstin")" />

        <DxGridDataColumn FieldName="Panno" Caption="PAN No."
                          Visible="@GetColumnVisibility("Panno")"
                          Width="@GetColumnWidth("Panno")" />

        <DxGridDataColumn FieldName="IsGstApplicable" Caption="GST Applicable"
                          Visible="@GetColumnVisibility("IsGstApplicable")"
                          Width="@GetColumnWidth("IsGstApplicable")" />

        <DxGridDataColumn FieldName="Status" Caption="Status"/>
    </Columns>
</DxGrid>

@if (showLayoutDialog)
{
    <div class="modal fade show d-block"
         style="background-color: rgba(0, 0, 0, 0.5); z-index: 1050;"
         @onclick="@(() => { })">

        <div class="modal-dialog modal-lg" @onclick:stopPropagation="true">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">@dialogTitle</h5>
                    <button type="button"
                            class="btn-close"
                            @onclick="CloseLayoutDialog"
                            aria-label="Close">
                    </button>
                </div>

                <div class="modal-body">
                    <div class="mb-3">
                        <label class="form-label fw-bold">Layout Name:</label>
                        <input type="text"
                               class="form-control"
                               @bind="newLayoutName"
                               @bind:event="oninput"
                               placeholder="Enter layout name..."
                               maxlength="100" />
                    </div>

                    <div class="mb-3">
                        <div class="form-check">
                            <input class="form-check-input"
                                   type="checkbox"
                                   @bind="isDefaultLayout"
                                   id="defaultLayoutCheck" />
                            <label class="form-check-label" for="defaultLayoutCheck">
                                Set as Default Layout
                            </label>
                        </div>
                    </div>

                    <div class="mt-3">
                        <label class="form-label fw-bold mb-2">Column Configuration:</label>
                        <div class="table-responsive" style="max-height: 400px; overflow-y: auto;">
                            <table class="table table-sm table-bordered">
                                <thead class="table-light sticky-top">
                                    <tr>
                                        <th style="width: 50px;">Visible</th>
                                        <th style="width: 200px;">Column Name</th>
                                        <th style="width: 150px;">Width (px)</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var col in allColumns)
                                    {
                                        <tr>
                                            <td class="text-center">
                                                <input class="form-check-input"
                                                       type="checkbox"
                                                       @bind="col.Visible"
                                                       id="col_@col.FieldName" />
                                            </td>
                                            <td>
                                                <label class="mb-0" for="col_@col.FieldName">
                                                    @col.Caption
                                                </label>
                                            </td>
                                            <td>
                                                <input type="number"
                                                       class="form-control form-control-sm"
                                                       @bind="col.Width"
                                                       @bind:event="oninput"
                                                       min="50"
                                                       max="500"
                                                       step="10"
                                                       placeholder="Width" />
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>

                    <div class="alert alert-info mt-3">
                        <i class="bi bi-info-circle me-2"></i>
                        <strong>Note:</strong>
                        Adjust column widths here manually. The widths will be saved and applied when you load this layout.
                    </div>
                </div>

                <div class="modal-footer">
                    <button type="button"
                            class="btn btn-secondary"
                            @onclick="CloseLayoutDialog">
                        Cancel
                    </button>
                    <button type="button"
                            class="btn btn-primary"
                            @onclick="SaveCurrentLayout">
                        <i class="bi bi-floppy me-1"></i>
                        @(isEditMode ? "Update" : "Create") Layout
                    </button>
                </div>
            </div>
        </div>
    </div>
}

<CompanyAddEditPage @ref="companyPopup" OnSaved="LoadCompaniesAsync" />
<ConfigAddEditPage @ref="configPopup" OnSaved="LoadCompaniesAsync" />
<ConfirmationDialog />

@code {
    DxGrid CompanyGrid;
    private List<Company> companyList = [];
    // private List<Company> SelectedCompanies = new();
    private IReadOnlyList<object> SelectedCompanies { get; set; } = new List<object>();
    private CompanyAddEditPage companyPopup;
    private ConfigAddEditPage configPopup;
    private EinvConfig ConfigModel = new();

    // Grid Layout Management Variables
    private List<GridLayoutDto> availableLayouts = [];
    private int? selectedLayoutId;
    private bool showLayoutDialog = false;
    private string newLayoutName = "";
    private string dialogTitle = "";
    private bool isEditMode = false;
    private bool isDefaultLayout = false;
    private const string VOUCHER_TYPE = "Company";

    private List<GridColumnLayout> allColumns = [];
    private List<GridColumnLayout> currentLayout = [];

    protected override async Task OnInitializedAsync()
    {
        await LoadCompaniesAsync();
        InitializeColumns();
        await LoadAvailableLayouts();
        await LoadDefaultLayout();
    }

    private void InitializeColumns()
    {
        allColumns = new List<GridColumnLayout>
        {
            new() { FieldName = "CompanyId", Caption = "ID", Visible = false, Width = 80, DisplayIndex = 0 },
            new() { FieldName = "Name", Caption = "Name", Visible = true, Width = 300, DisplayIndex = 1 },
            new() { FieldName = "Code", Caption = "Code", Visible = false, Width = 120, DisplayIndex = 2 },
            new() { FieldName = "PrintName", Caption = "Print Name", Visible = false, Width = 200, DisplayIndex = 3 },
            new() { FieldName = "Address1", Caption = "Address", Visible = true, Width = 250, DisplayIndex = 4 },
            new() { FieldName = "City", Caption = "City", Visible = true, Width = 150, DisplayIndex = 5 },
            new() { FieldName = "StateName", Caption = "State", Visible = false, Width = 120, DisplayIndex = 6 },
            new() { FieldName = "Country", Caption = "Country", Visible = false, Width = 120, DisplayIndex = 7 },
            new() { FieldName = "Email", Caption = "Email", Visible = true, Width = 200, DisplayIndex = 8 },
            new() { FieldName = "Mobile", Caption = "Mobile", Visible = false, Width = 150, DisplayIndex = 9 },
            new() { FieldName = "Website", Caption = "Website", Visible = false, Width = 150, DisplayIndex = 10 },
            new() { FieldName = "Gstin", Caption = "GSTIN", Visible = true, Width = 180, DisplayIndex = 11 },
            new() { FieldName = "Panno", Caption = "PAN No.", Visible = false, Width = 150, DisplayIndex = 12 },
            new() { FieldName = "IsGstApplicable", Caption = "GST Applicable", Visible = false, Width = 150, DisplayIndex = 13 },
            // new() { FieldName = "Status", Caption = "Status", Visible = false, Width = 120, DisplayIndex = 14 }
        };

        currentLayout = allColumns.Select(c => new GridColumnLayout
        {
            FieldName = c.FieldName,
            Caption = c.Caption,
            Visible = c.Visible,
            Width = c.Width,
            DisplayIndex = c.DisplayIndex
        }).ToList();
    }

    private int ParseWidth(string width)
    {
        if (string.IsNullOrEmpty(width))
            return 100;

        width = width.Replace("px", "").Trim();

        if (int.TryParse(width, out int result))
            return result;

        return 100;
    }

    private string GetColumnWidth(string fieldName)
    {
        if (currentLayout == null || !currentLayout.Any())
        {
            var defaultCol = allColumns.FirstOrDefault(x => x.FieldName == fieldName);
            return $"{defaultCol?.Width ?? 100}px";
        }

        var col = currentLayout.FirstOrDefault(x => x.FieldName == fieldName);
        return $"{col?.Width ?? 100}px";
    }

    private bool GetColumnVisibility(string fieldName)
    {
        if (currentLayout == null || !currentLayout.Any())
        {
            return true;
        }

        var col = currentLayout.FirstOrDefault(x => x.FieldName == fieldName);
        return col?.Visible ?? true;
    }

    private async Task LoadAvailableLayouts()
    {
        availableLayouts = await GridLayoutService.GetLayoutsByVoucherTypeAsync(VOUCHER_TYPE);
    }

    private async Task LoadDefaultLayout()
    {
        var defaultLayout = await GridLayoutService.GetDefaultLayoutAsync(VOUCHER_TYPE);
        if (defaultLayout != null)
        {
            selectedLayoutId = defaultLayout.GridLayoutId;
            await ApplyLayout(defaultLayout);
        }
    }

    private async Task OnLayoutSelectedAsync(int? layoutId)
    {
        selectedLayoutId = layoutId;

        if (layoutId.HasValue)
        {
            var layout = await GridLayoutService.GetByIdAsync(layoutId.Value);
            if (layout != null)
            {
                await ApplyLayout(layout);
            }
        }
        else
        {
            InitializeColumns();
            await InvokeAsync(StateHasChanged);
        }
    }

    private async Task ApplyLayout(GridLayoutDto layout)
    {
        if (!string.IsNullOrEmpty(layout.GridLayoutData))
        {
            var savedLayout = GridLayoutService.DeserializeColumnLayout(layout.GridLayoutData);
            if (savedLayout != null && savedLayout.Any())
            {
                currentLayout = savedLayout.Select(s => new GridColumnLayout
                {
                    FieldName = s.FieldName,
                    Caption = s.Caption,
                    Visible = s.Visible,
                    Width = s.Width,
                    DisplayIndex = s.DisplayIndex
                }).ToList();

                foreach (var saved in savedLayout)
                {
                    var existing = allColumns.FirstOrDefault(x => x.FieldName == saved.FieldName);
                    if (existing != null)
                    {
                        existing.Visible = saved.Visible;
                        existing.Width = saved.Width;
                        existing.DisplayIndex = saved.DisplayIndex;
                    }
                }

                await InvokeAsync(StateHasChanged);
            }
        }
    }

    private void ShowSaveLayoutDialog()
    {
        if (selectedLayoutId.HasValue)
        {
            var layout = availableLayouts.FirstOrDefault(x => x.GridLayoutId == selectedLayoutId.Value);
            if (layout != null)
            {
                newLayoutName = layout.GridLayoutName ?? "";
                isDefaultLayout = layout.GridLayoutDefault;
                isEditMode = true;
                dialogTitle = "Update Grid Layout";
                showLayoutDialog = true;
                return;
            }
        }

        ShowNewLayoutDialog();
    }

    private void ShowNewLayoutDialog()
    {
        newLayoutName = "";
        isDefaultLayout = false;
        isEditMode = false;
        dialogTitle = "Create New Grid Layout";
        showLayoutDialog = true;
    }

    private async Task SaveCurrentLayout()
    {
        if (string.IsNullOrWhiteSpace(newLayoutName))
        {
            ToastService.Warning("Please enter a layout name.");
            return;
        }

        LoadingService.Show("Saving layout...");
        try
        {
            foreach (var col in allColumns)
            {
                var current = currentLayout.FirstOrDefault(c => c.FieldName == col.FieldName);
                if (current != null)
                {
                    current.Visible = col.Visible;
                    current.Width = col.Width;
                    current.DisplayIndex = col.DisplayIndex;
                }
                else
                {
                    currentLayout.Add(new GridColumnLayout
                    {
                        FieldName = col.FieldName,
                        Caption = col.Caption,
                        Visible = col.Visible,
                        Width = col.Width,
                        DisplayIndex = col.DisplayIndex
                    });
                }
            }

            var configJson = GridLayoutService.SerializeColumnLayout(currentLayout);
            bool success = false;

            if (isEditMode && selectedLayoutId.HasValue)
            {
                var updateRequest = new SaveGridLayoutRequest
                {
                    GridLayoutName = newLayoutName,
                    GridLayoutVoucherType = VOUCHER_TYPE,
                    GridLayoutData = configJson,
                    GridLayoutDefault = isDefaultLayout
                };
                success = await GridLayoutService.UpdateAsync(selectedLayoutId.Value, updateRequest);
            }
            else
            {
                var newLayoutRequest = new SaveGridLayoutRequest
                {
                    GridLayoutName = newLayoutName,
                    GridLayoutVoucherType = VOUCHER_TYPE,
                    GridLayoutData = configJson,
                    GridLayoutDefault = isDefaultLayout
                };
                var result = await GridLayoutService.CreateAsync(newLayoutRequest);
                success = result;

                if (success)
                {
                    await LoadAvailableLayouts();
                    var createdLayout = availableLayouts.FirstOrDefault(l => l.GridLayoutName == newLayoutName);
                    if (createdLayout != null)
                        selectedLayoutId = createdLayout.GridLayoutId;
                }
            }

            if (success)
            {
                ToastService.Success($"Grid layout {(isEditMode ? "updated" : "created")} successfully!");
                await LoadAvailableLayouts();

                if (isDefaultLayout)
                {
                    await LoadDefaultLayout();
                }

                CloseLayoutDialog();
                await InvokeAsync(StateHasChanged);
            }
            else
            {
                ToastService.Error($"Failed to {(isEditMode ? "update" : "create")} grid layout.");
            }
        }
        catch (Exception ex)
        {
            ToastService.Error($"Error saving layout: {ex.Message}");
            Console.WriteLine($"Layout save error: {ex}");
        }
        finally
        {
            LoadingService.Hide();
        }
    }

    private async Task DeleteCurrentLayout()
    {
        if (!selectedLayoutId.HasValue) return;

        var confirm = await ConfirmDialogService.ShowAsync(
            "Are you sure you want to delete this grid layout?");

        if (!confirm) return;

        LoadingService.Show("Deleting layout...");
        try
        {
            var success = await GridLayoutService.DeleteAsync(selectedLayoutId.Value);

            if (success)
            {
                ToastService.Success("Grid layout deleted successfully!");

                selectedLayoutId = null;
                InitializeColumns();
                currentLayout = allColumns.ToList();

                await LoadAvailableLayouts();
                await InvokeAsync(StateHasChanged);
            }
            else
            {
                ToastService.Error("Failed to delete grid layout.");
            }
        }
        catch (Exception ex)
        {
            ToastService.Error($"Error deleting layout: {ex.Message}");
        }
        finally
        {
            LoadingService.Hide();
        }
    }

    private void CloseLayoutDialog()
    {
        showLayoutDialog = false;
        newLayoutName = "";
        isDefaultLayout = false;
        isEditMode = false;
    }
    // ✅ FIXED: Reliable clear selection method
    private async Task ClearSelectionManual()
    {
        try
        {
            if (CompanyGrid != null)
            {
                CompanyGrid.ClearSelection(); // ✅ Synchronous method
            }
            SelectedCompanies = new List<Company>();
            await InvokeAsync(StateHasChanged);
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Clear selection error: {ex.Message}");
        }
    }

    // ✅ FIXED: Auto-clear after operations
    private async Task ClearSelection()
    {
        await Task.Delay(100); // Small delay for UI update
        await ClearSelectionManual();
    }
    private async Task LoadCompaniesAsync()
    {
        LoadingService.Show();
        try
        {
            companyList = await CompanyService.GetAllAsync();
            var configs = await EinvConfigService.GetAllAsync();
            ConfigModel = configs.FirstOrDefault() ?? new EinvConfig();
        }
        finally
        {
            LoadingService.Hide();
        }
    }

    private async Task OnSelectedCompaniesChanged(IReadOnlyList<object> companies)
    {
        SelectedCompanies = companies ?? new List<object>();
        await InvokeAsync(StateHasChanged);
    }

    private void OnCreateNew(ToolbarItemClickEventArgs e)
    {
        companyPopup.Show(new Company());
    }

    private async Task OnEdit(ToolbarItemClickEventArgs e)
    {
        var company = CompanyGrid.GetFocusedDataItem() as Company;
        if (company != null)
            companyPopup.Show(company);
        else
            ToastService.Warning("Please select a company to edit.");
        await ClearSelection();
    }

    private async Task OnDelete(ToolbarItemClickEventArgs e)
    {
        if (SelectedCompanies == null || SelectedCompanies.Count == 0)
        {
            ToastService.Warning("Please select at least one company to delete.");
            return;
        }
        var selectedList = SelectedCompanies.Cast<Company>().ToList();
        var confirm = await ConfirmDialogService.ShowAsync(
            $"Are you sure you want to delete {SelectedCompanies.Count} selected company(s)?");

        if (confirm)
        {
            LoadingService.Show();
            try
            {
                foreach (var company in selectedList)
                {
                    await CompanyService.DeleteAsync(company.CompanyId);
                }

                ToastService.Success($"{SelectedCompanies.Count} company(s) deleted successfully.");
                await LoadCompaniesAsync();
            }
            catch (Exception ex)
            {
                ToastService.Error($"Error deleting companies: {ex.Message}");
            }
            finally
            {
                LoadingService.Hide();
                await ClearSelection();
            }
        }
    }

    private void OnConfig(ToolbarItemClickEventArgs e)
    {
        configPopup.Show(ConfigModel);
    }
}