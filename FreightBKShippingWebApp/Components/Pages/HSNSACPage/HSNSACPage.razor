@page "/hsnsac"
  
@using FreightBKShippingWebApp.Model
@using FreightBKShippingWebApp.Services
@using FreightBKShippingWebApp.Components.Pages
@inject HsnSacService HsnSacService
@inject ToasteService ToastService
@inject LoadingService LoadingService
@inject ConfirmationDialogService ConfirmDialogService
@inject GridLayoutService GridLayoutService
@inject AuditLogService AuditLogService
    <style >
    .grid-size {
        max-height: 600px;
    }


    /* ComboBox Styling */
    .layout-combobox {
        border-radius: 6px;
        width: 185px;
    }
</style>
<DxGrid TItem="HsnSacModel" Data="@hsnList" @ref="MyGrid" 
        KeyFieldName="HsnId" 
        ShowSearchBox="true"
        PageSize="10"
        PageSizeSelectorVisible="true"
        ShowGroupPanel="true"
        FilterMenuButtonDisplayMode="GridFilterMenuButtonDisplayMode.Always"
        FocusedRowEnabled="true"
        SelectionMode="GridSelectionMode.Multiple"
        SelectedDataItems="@SelectedHsn"
        SelectedDataItemsChanged="OnSelectedHsnChanged"
        ColumnResizeMode="GridColumnResizeMode.NextColumn"
        CssClass="grid-size"
        AllowSelectRowByClick="true">
    <ToolbarTemplate Context="toolbarContext">
        <DxToolbar>
            <TitleTemplate Context="tl">
                <div class="d-flex align-items-center">
                    <i class="bi bi-tags me-2"></i>
                    <span class="fs-5 ms-2">HSN/SAC</span>
                </div>
            </TitleTemplate>
            <Items>
                <DxToolbarItem Text="Add" IconCssClass="bi bi-plus-circle" Click="OnCreateNew" />
                <DxToolbarItem Text="Edit" IconCssClass="bi bi-pencil-square" Click="OnEdit" />
                <DxToolbarItem Text="Delete" IconCssClass="bi bi-trash" Click="OnDelete" />
                <DxToolbarItem Text="Refresh" IconCssClass="bi bi-arrow-clockwise" Click="@(async (_) => await LoadHsnAsync())" />

                <DxToolbarItem Alignment="ToolbarItemAlignment.Right"
                               RenderStyle="ButtonRenderStyle.Secondary"
                               IconCssClass="bi bi-gear-wide-connected"
                               Click="ShowLayoutPopup" />

            </Items>
        </DxToolbar>
    </ToolbarTemplate>

    <Columns>
        <DxGridSelectionColumn Width="30" FixedPosition="GridColumnFixedPosition.Left" />

        <DxGridDataColumn FieldName="HsnId" Caption="ID"
                          Width="@(layoutManager?.GetColumnWidth("HsnId") ?? "80px")"
                          Visible="@(layoutManager?.GetColumnVisibility("HsnId") ?? false)" />

        <DxGridDataColumn FieldName="HsnName" Caption="HSN/SAC Name"
                          Width="@(layoutManager?.GetColumnWidth("HsnName") ?? "400px")"
                          Visible="@(layoutManager?.GetColumnVisibility("HsnName") ?? true)" />

        <DxGridDataColumn FieldName="HsnGstPer" Caption="GST %"
                          DisplayFormat="n2"
                          Width="@(layoutManager?.GetColumnWidth("HsnGstPer") ?? "230px")"
                          Visible="@(layoutManager?.GetColumnVisibility("HsnGstPer") ?? true)" />

        <DxGridDataColumn FieldName="HsnCessType" Caption="Cess Type"
                          Width="@(layoutManager?.GetColumnWidth("HsnCessType") ?? "270px")"
                          Visible="@(layoutManager?.GetColumnVisibility("HsnCessType") ?? true)" />

        <DxGridDataColumn FieldName="HsnCess" Caption="Cess %"
                          DisplayFormat="n2"
                          Width="@(layoutManager?.GetColumnWidth("HsnCess") ?? "220px")"
                          Visible="@(layoutManager?.GetColumnVisibility("HsnCess") ?? true)" />

        <DxGridDataColumn FieldName="HsnAddCess" Caption="Addl Cess %"
                          DisplayFormat="n2"
                          Width="@(layoutManager?.GetColumnWidth("HsnAddCess") ?? "220px")"
                          Visible="@(layoutManager?.GetColumnVisibility("HsnAddCess") ?? true)" />

        <DxGridDataColumn FieldName="HsnStatus" Caption="Active"
                          Width="@(layoutManager?.GetColumnWidth("HsnStatus") ?? "120px")"
                          Visible="@(layoutManager?.GetColumnVisibility("HsnStatus") ?? true)" />
    </Columns>
</DxGrid>

<HsnSacAddEditPage @ref="popup" OnSaved="LoadHsnAsync" />

<ConfirmationDialog />
<GridLayoutpPopup @ref="layoutManager"
                  VoucherType="Hsnsac"
                  Columns="@allColumns"
                  OnLayoutChanged="HandleLayoutChanged" />

@code {
    DxGrid MyGrid;
    private List<HsnSac> hsnList = [];
    // private List<HsnSac> SelectedHsn = new();
    private IReadOnlyList<object> SelectedHsn { get; set; } = new List<object>();

    private HsnSacAddEditPage popup;


    // All possible columns
    private GridLayoutpPopup? layoutManager;
    private List<GridColumnLayout> allColumns = [];
    private async void ShowLayoutPopup()
    {
        await InvokeAsync(() =>
        {
            layoutManager?.ShowPopup();
        });
    }
    private async Task HandleLayoutChanged(List<GridColumnLayout> updatedLayout)
    {
        allColumns = updatedLayout;
        // Force the grid to re-render with new layout
        await InvokeAsync(StateHasChanged);
    }


    protected override async Task OnInitializedAsync()
    {
        InitializeColumns();
        await LoadHsnAsync();


    }
    private void InitializeColumns()
    {
        allColumns = new List<GridColumnLayout>
        { 
            new() { FieldName = "HsnId", Caption = "ID", Visible = false, Width = 80, DisplayIndex = 0 },

               new() { FieldName = "HsnName", Caption = "HSN/SAC Name",Visible = true, Width = 400, DisplayIndex = 1 },

    new() { FieldName = "HsnGstPer", Caption = "GST %",Visible = true, Width = 230, DisplayIndex = 2 },

    new() { FieldName = "HsnCessType", Caption = "Cess Type",Visible = true, Width = 270, DisplayIndex = 3 },

    new() { FieldName = "HsnCess", Caption = "Cess %",Visible = true, Width = 220, DisplayIndex = 4 },

    new() { FieldName = "HsnAddCess", Caption = "Addl Cess %",Visible = true, Width = 220, DisplayIndex = 5 },

     new() { FieldName = "HsnStatus", Caption = "Active",Visible = true, Width = 120, DisplayIndex = 6 }
        };

    }


    private async Task LoadHsnAsync()
    {
        LoadingService.Show();
        try
        {
            hsnList = await HsnSacService.GetAllAsync();
        }
        finally
        {
            LoadingService.Hide();
        }
    }

    private async Task OnSelectedHsnChanged(IReadOnlyList<object> items)
    {
        SelectedHsn = items ?? new List<object>();
        await InvokeAsync(StateHasChanged);
    }
    // ✅ FIXED: Reliable clear selection method
    private async Task ClearSelectionManual()
    {
        try
        {
            if (MyGrid != null)
            {
                MyGrid.ClearSelection(); // ✅ Synchronous method
            }
            SelectedHsn = new List<HsnSac>();
            await InvokeAsync(StateHasChanged);
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Clear selection error: {ex.Message}");
        }
    }

    // ✅ FIXED: Auto-clear after operations
    private async Task ClearSelection()
    {
        await Task.Delay(100); // Small delay for UI update
        await ClearSelectionManual();
    }

    private void OnCreateNew(ToolbarItemClickEventArgs e)
    {
        popup.Show(new HsnSac() { HsnStatus = true });
    }

    private async Task OnEdit(ToolbarItemClickEventArgs e)
    {
        var hsn = MyGrid.GetFocusedDataItem() as HsnSac;
        if (hsn != null)
        {
            var freshData = hsnList
             .FirstOrDefault(x => x.HsnId == hsn.HsnId);

            if (freshData != null)
                popup.Show(freshData);
        }
        
        else
            ToastService.Warning("Please select a record to edit.");
        await ClearSelection();
    }

    private async Task OnDelete(ToolbarItemClickEventArgs e)
    {
        if (SelectedHsn == null || SelectedHsn.Count == 0)
        {
            ToastService.Warning("Please select at least one record to delete.");
            return;
        }
        var selectedList = SelectedHsn.Cast<HsnSac>().ToList();

        var confirm = await ConfirmDialogService.ShowAsync(
            $"Are you sure you want to delete {SelectedHsn.Count} selected record(s)?");

        if (!confirm)
        {
            await ClearSelection(); // ✅ Clear on cancel
            return;
        }
        try
        {
            LoadingService.Show("Deleting...");
            int successCount = 0, failCount = 0;
            foreach (var item in selectedList)
        {
            var result = await HsnSacService.DeleteAsync(item.HsnId);
            if (result)
{
                successCount++;
                    AuditLogService.NotifyAuditChanged();
} else
                failCount++;
        }

        if (successCount > 0)
            ToastService.Success($"{successCount} record(s) deleted successfully.");
        if (failCount > 0)
            ToastService.Error($"{failCount} record(s) failed to delete.");

        await LoadHsnAsync();
        }
        finally
        {
            LoadingService.Hide();
            await ClearSelection(); // ✅ Clear after delete (success or fail)
        }
    }
}
