@page "/user"
@attribute [Authorize]
@using Microsoft.AspNetCore.Authorization
@using FreightBKShippingWebApp.Model
@using FreightBKShippingWebApp.Services
@inject UserService UserService
@inject ToasteService ToastService
@inject LoadingService LoadingService
@inject ConfirmationDialogService ConfirmDialogService
@inject GridLayoutService GridLayoutService
@inject AuditLogService AuditLogService
<style>
    .grid-size {
        max-height: 600px;
    }


    /* ComboBox Styling */
    .layout-combobox {
        border-radius: 6px;
        width: 185px;
    }
</style>
<DxGrid TItem="User"
        Data="@userList"
        @ref="MyGrid"
        KeyFieldName="UserId"
        ShowSearchBox="true"
        PageSize="10"
        PageSizeSelectorVisible="true"
        ShowGroupPanel="true"
        FilterMenuButtonDisplayMode="GridFilterMenuButtonDisplayMode.Always"
        FocusedRowEnabled="true"
        SelectionMode="GridSelectionMode.Multiple"
        SelectedDataItems="@SelectedUsers"
        SelectedDataItemsChanged="OnSelectedUsersChanged"
        ColumnResizeMode="GridColumnResizeMode.NextColumn"
        CssClass="grid-size"
        AllowSelectRowByClick="true">

    <ToolbarTemplate>
        <DxToolbar>
            <TitleTemplate Context="tl">
                <div class="d-flex align-items-center">
                    <i class="bi bi-people me-2"></i>
                    <span class="fs-5">Users</span>
                </div>
            </TitleTemplate>
            <Items>
                <DxToolbarItem Text="Add" IconCssClass="bi bi-plus-circle" Click="OnCreateNew" />
                <DxToolbarItem Text="Edit" IconCssClass="bi bi-pencil-square" Click="OnEdit" />
                <DxToolbarItem Text="Delete" IconCssClass="bi bi-trash" Click="OnDelete" />
                <DxToolbarItem Text="Refresh" IconCssClass="bi bi-arrow-clockwise" Click="@LoadUsersAsync" />
                <DxToolbarItem Alignment="ToolbarItemAlignment.Right"
                               RenderStyle="ButtonRenderStyle.Secondary"
                               IconCssClass="bi bi-gear-wide-connected"
                               Click="ShowLayoutPopup" />
            </Items>
        </DxToolbar>
    </ToolbarTemplate>

    <Columns>

        <DxGridSelectionColumn Width="30" FixedPosition="GridColumnFixedPosition.Left" />

        <DxGridDataColumn FieldName="UserId" Caption="ID"
                          Width="@(layoutManager?.GetColumnWidth("UserId") ?? "80px")"
                          Visible="@(layoutManager?.GetColumnVisibility("UserId") ?? false)" />

        <DxGridDataColumn FieldName="UserName" Caption="User Name"
                          Width="@(layoutManager?.GetColumnWidth("UserName") ?? "260px")"
            Visible="@(layoutManager?.GetColumnVisibility("UserName") ?? true)" />
     

        <DxGridDataColumn FieldName="UserFirstName" Caption="First Name"
                          Width="@(layoutManager?.GetColumnWidth("UserFirstName") ?? "200px")"
                          Visible="@(layoutManager?.GetColumnVisibility("UserFirstName") ?? true)" />

        <DxGridDataColumn FieldName="UserLastName" Caption="Last Name"
                          Width="@(layoutManager?.GetColumnWidth("UserLastName") ?? "250px")"
                          Visible="@(layoutManager?.GetColumnVisibility("UserLastName") ?? true)" />

        <DxGridDataColumn FieldName="UserEmail" Caption="Email"
                          Width="@(layoutManager?.GetColumnWidth("UserEmail") ?? "270px")"
                          Visible="@(layoutManager?.GetColumnVisibility("UserEmail") ?? true)" />

        <DxGridDataColumn FieldName="UserMobile" Caption="Mobile"
                          Width="@(layoutManager?.GetColumnWidth("UserMobile") ?? "200px")"
                          Visible="@(layoutManager?.GetColumnVisibility("UserMobile") ?? true)" />

        <DxGridDataColumn FieldName="UserCreated" Caption="Created On"
                          DisplayFormat="dd/MM/yyyy"
                          Width="@(layoutManager?.GetColumnWidth("UserCreated") ?? "160px")"
                          Visible="@(layoutManager?.GetColumnVisibility("UserCreated") ?? true)" />

        <DxGridDataColumn FieldName="UserStatus" Caption="Status"
                          Width="@(layoutManager?.GetColumnWidth("UserStatus") ?? "120px")"
                          Visible="@(layoutManager?.GetColumnVisibility("UserStatus") ?? true)" />

    </Columns>
</DxGrid>

<UserAddEditPage @ref="popup" OnSaved="LoadUsersAsync" />
<ConfirmationDialog />
<GridLayoutpPopup @ref="layoutManager"
                  VoucherType="User"
                  Columns="@allColumns"
                  OnLayoutChanged="HandleLayoutChanged" />
@code {
    DxGrid MyGrid;
    private List<User> userList = new();
    // private List<User> SelectedUsers = new();
    private IReadOnlyList<object> SelectedUsers { get; set; } = new List<object>();
    private UserAddEditPage popup;


    // All possible columns
    private GridLayoutpPopup? layoutManager;
    private List<GridColumnLayout> allColumns = [];
    private async void ShowLayoutPopup()
    {
        await InvokeAsync(() =>
        {
            layoutManager?.ShowPopup();
        });
    }
    private async Task HandleLayoutChanged(List<GridColumnLayout> updatedLayout)
    {
        allColumns = updatedLayout;
        // Force the grid to re-render with new layout
        await InvokeAsync(StateHasChanged);
    }

    protected override async Task OnInitializedAsync()
    {
        InitializeColumns();
        await LoadUsersAsync();


    }
    private void InitializeColumns()
    {
        allColumns = new List<GridColumnLayout>
    {
        new() { FieldName = "UserId", Caption = "ID", Visible = false, Width = 80, DisplayIndex = 0 },

        new() { FieldName = "UserName", Caption = "User Name", Visible = true, Width = 260, DisplayIndex = 1 },
        new() { FieldName = "UserFirstName", Caption = "First Name", Visible = true, Width = 200, DisplayIndex = 2 },
        new() { FieldName = "UserLastName", Caption = "Last Name", Visible = true, Width = 250, DisplayIndex = 3 },

        new() { FieldName = "UserEmail", Caption = "Email", Visible = true, Width = 270, DisplayIndex = 4 },
        new() { FieldName = "UserMobile", Caption = "Mobile", Visible = true, Width = 200, DisplayIndex = 5 },

         new() { FieldName = "UserStatus", Caption = "Status", Visible = true, Width = 120, DisplayIndex = 6 },
        new() { FieldName = "UserCreated", Caption = "Created On", Visible = true, Width = 160, DisplayIndex = 7 }
    };

    }


    private async Task LoadUsersAsync()
    {
        LoadingService.Show();
        try
        {
            userList = await UserService.GetAllAsync();
        }
        finally
        {
            LoadingService.Hide();
        }
    }

    private async Task OnSelectedUsersChanged(IReadOnlyList<object> users)
    {
        SelectedUsers = users ?? new List<object>();
        await InvokeAsync(StateHasChanged);
    }
    // ✅ FIXED: Reliable clear selection method
    private async Task ClearSelectionManual()
    {
        try
        {
            if (MyGrid != null)
            {
                MyGrid.ClearSelection(); // ✅ Synchronous method
            }
            SelectedUsers = new List<User>();
            await InvokeAsync(StateHasChanged);
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Clear selection error: {ex.Message}");
        }
    }

    // ✅ FIXED: Auto-clear after operations
    private async Task ClearSelection()
    {
        await Task.Delay(100); // Small delay for UI update
        await ClearSelectionManual();
    }

    private void OnCreateNew(ToolbarItemClickEventArgs e)
    {
        popup.Show(new User(),true);
    }

    private async Task OnEdit(ToolbarItemClickEventArgs e)
    {
        var user = MyGrid.GetFocusedDataItem() as User;
        if (user != null)
        {
            var freshData = userList
               .FirstOrDefault(x => x.UserId == user.UserId);

            if (freshData != null)
                popup.Show(freshData, false);
        }
        else
            ToastService.Warning("Please select a user to edit.");


        await ClearSelection();
    }

    private async Task OnDelete(ToolbarItemClickEventArgs e)
    {
        if (SelectedUsers == null || SelectedUsers.Count == 0)
        {
            ToastService.Warning("Please select at least one user to delete.");
            return;
        }
        var selectedList = SelectedUsers.Cast<User>().ToList();
        var confirm = await ConfirmDialogService.ShowAsync(
            $"Are you sure you want to delete {SelectedUsers.Count} selected user(s)?");
        if (!confirm)
        {
            await ClearSelection(); // ✅ Clear on cancel
            return;
        }
        try
        {
            LoadingService.Show("Deleting...");
            int successCount = 0, failCount = 0;
            List<string> errorMessages = new();
            foreach (var user in selectedList)
            {
                var result = await UserService.DeleteAsync(user.UserId);
                if (result.Success)
                {
                    successCount++;
                    AuditLogService.NotifyAuditChanged();

                }
                else
                {
                    failCount++;
                    errorMessages.Add(result.Error);
                }
            }
            if (successCount > 0) ToastService.Success($"{successCount} User(s) Deleted Successfully.");
            if (failCount > 0) ToastService.Error($"{failCount} User(s) Failed To Delete.\nDetails: {string.Join("\n", errorMessages.Distinct())}");

            await LoadUsersAsync();
        }
        finally
        {
            LoadingService.Hide();
            await ClearSelection();
        }
    }
}
